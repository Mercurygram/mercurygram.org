+++
title = "Features"
description = "Android and Desktop are separate forks with separate option sets. Pick the client you run."
sort_by = "weight"

# Maintenance: the cards below are generated from the pages in this directory,
# so a new client page needs no template change. `weight` orders them and the
# page `description` becomes the card text.
+++

Mercurygram is two forks, not one app on two platforms: the Android client
descends from Telegram for Android, the Desktop client from Telegram Desktop.
They share the same goals and a good deal of behaviour, but each carries its own
patches, so the options differ. Pick the one you run.
